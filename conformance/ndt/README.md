# Conformidade NDT

Os exemplos válidos encontram-se em `specs/ndt/examples/` e são executados
como parte da suite NDT. Este diretório contém casos que DEVEM ser rejeitados.

```bash
python3 tools/validate.py --format ndt
```
