{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://normordis.pt/specs/registry/informacao-tecnica/1.0.0/schema.json",
  "title": "Informação Técnica v1.0.0",
  "description": "Schema do campo 'documento' para tipo_documento_ref: 'informacao-tecnica@1.0.0'. Nota interna fundamentada — análise, parecer ou proposta de decisão com fundamentação técnica.",
  "type": "object",
  "required": ["numero", "data", "dirigido_a", "assunto", "corpo"],
  "additionalProperties": false,
  "properties": {
    "numero": {
      "type": "string",
      "minLength": 1,
      "description": "Número da informação técnica (ex.: 'IT/2026/00045').",
      "examples": ["IT/2026/00045"]
    },
    "data": {
      "type": "string",
      "format": "date",
      "description": "Data de produção (ISO 8601, YYYY-MM-DD)."
    },
    "dirigido_a": {
      "type": "string",
      "minLength": 1,
      "description": "Destinatário interno: cargo ou unidade orgânica a quem a informação é dirigida.",
      "examples": ["Exmo. Sr. Director-Geral", "Divisão de Gestão Documental"]
    },
    "assunto": {
      "type": "string",
      "minLength": 1,
      "description": "Assunto da informação técnica.",
      "examples": ["Análise de conformidade do sistema X com a Lei n.º 58/2019"]
    },
    "referencias": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Referências a documentos, processos ou diplomas legais relevantes."
    },
    "corpo": {
      "type": "string",
      "minLength": 1,
      "description": "Corpo da informação técnica — análise, fundamentação e, quando aplicável, proposta de decisão."
    },
    "conclusao": {
      "type": "string",
      "description": "Síntese conclusiva ou proposta — destacada do corpo para facilitar a leitura pelo destinatário."
    },
    "proposta_despacho": {
      "type": "string",
      "description": "Texto sugerido para despacho de concordância ou decisão pelo destinatário (opcional)."
    },
    "autor": {
      "type": "object",
      "required": ["nome", "cargo"],
      "properties": {
        "nome": { "type": "string" },
        "cargo": { "type": "string" },
        "unidade_organica": { "type": "string" },
        "delegacao_ref": {
          "type": "string",
          "description": "Referência ao despacho de delegação que habilita o autor, quando emite a informação técnica por delegação de competência."
        }
      },
      "additionalProperties": false,
      "description": "Autor da informação técnica."
    },
    "componentes": {
      "type": "array",
      "description": "Componentes binários associados a este documento — tipicamente anexos (papel 'anexo'). O conteúdo do ato vive nos campos estruturados deste schema; os componentes são material que o acompanha e que não nasceu no editor estruturado. O hash entra nos payload_bytes e fica coberto pela assinatura (ADR-021). Nenhuma entrada declara localização de armazenamento: a resolução faz-se por hash. Um anexo com identidade documental própria NÃO DEVE ser declarado aqui — é NDF autónomo, ligado por relacoes[] (SPEC.md §2.8.1.3).",
      "items": {
        "type": "object",
        "required": [
          "id",
          "papel",
          "media_type",
          "sha256",
          "tamanho"
        ],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9_-]*$",
            "description": "Identificador do componente, unívoco dentro deste documento."
          },
          "papel": {
            "enum": [
              "original",
              "representacao_congelada",
              "anexo",
              "evidencia"
            ],
            "description": "original — bytes tal como emitidos ou recebidos, sempre preservados. representacao_congelada — derivada de um original para garantir fidelidade visual, nunca o substitui. anexo — componente de apoio sem identidade documental própria. evidencia — material de validação de assinatura contida, congelado na captura. Ver docs/interoperability/PREMIS-METS-MAPPING.md §3."
          },
          "derivado_de": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9_-]*$",
            "description": "id do componente de origem. Obrigatório quando papel é representacao_congelada: uma representação congelada é sempre derivação de um original identificável."
          },
          "media_type": {
            "type": "string",
            "pattern": "^[a-z]+/[a-zA-Z0-9.+_-]+$",
            "description": "Tipo MIME determinado por inspeção do conteúdo, nunca inferido da extensão.",
            "examples": [
              "application/pdf",
              "image/jpeg",
              "application/vnd.oasis.opendocument.text"
            ]
          },
          "sha256": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9-]*:[0-9a-f]+$",
            "description": "Digest do componente no formato '<alg>:<hex>'. Valor normativo para NDF v1.x é sha256:<64 hex>. É a identidade do componente e a chave da sua resolução; DEVE constar de manifest.inventario num .ndfpkg (NDF-PKG-010)."
          },
          "tamanho": {
            "type": "integer",
            "minimum": 0,
            "description": "Dimensão em bytes."
          },
          "nome_original": {
            "type": "string",
            "minLength": 1,
            "description": "Nome do ficheiro tal como emitido ou recebido. É descritivo — NÃO é caminho dentro do pacote, e um leitor não o DEVE usar para resolver o componente."
          },
          "validacao_formato": {
            "type": "object",
            "required": [
              "resultado"
            ],
            "additionalProperties": false,
            "description": "Resultado da validação de formato no momento da captura. Registo, não juízo: um resultado nao_conforme não invalida o documento nem a captura (SPEC.md §2.8).",
            "properties": {
              "norma": {
                "type": "string",
                "description": "Norma de formato verificada.",
                "examples": [
                  "PDF/A",
                  "PDF/UA"
                ]
              },
              "parte": {
                "type": "string",
                "examples": [
                  "1",
                  "2",
                  "3",
                  "4"
                ]
              },
              "nivel": {
                "type": "string",
                "examples": [
                  "a",
                  "b",
                  "u"
                ]
              },
              "resultado": {
                "enum": [
                  "conforme",
                  "nao_conforme",
                  "nao_validavel"
                ],
                "description": "nao_validavel cobre ficheiro cifrado, corrompido ou sem validador disponível."
              },
              "validador": {
                "type": "object",
                "required": [
                  "nome",
                  "versao"
                ],
                "additionalProperties": false,
                "description": "Sem identificar o validador e a sua versão, um resultado registado hoje é ininterpretável a longo prazo.",
                "properties": {
                  "nome": {
                    "type": "string",
                    "minLength": 1
                  },
                  "versao": {
                    "type": "string",
                    "minLength": 1
                  }
                }
              },
              "verificado_em": {
                "type": "string",
                "format": "date-time"
              }
            }
          }
        },
        "allOf": [
          {
            "if": {
              "properties": {
                "papel": {
                  "const": "representacao_congelada"
                }
              },
              "required": [
                "papel"
              ]
            },
            "then": {
              "required": [
                "derivado_de"
              ]
            }
          },
          {
            "if": {
              "properties": {
                "validacao_formato": {
                  "properties": {
                    "resultado": {
                      "enum": [
                        "conforme",
                        "nao_conforme"
                      ]
                    }
                  },
                  "required": [
                    "resultado"
                  ]
                }
              },
              "required": [
                "validacao_formato"
              ]
            },
            "then": {
              "properties": {
                "validacao_formato": {
                  "required": [
                    "resultado",
                    "validador",
                    "verificado_em"
                  ]
                }
              }
            }
          }
        ]
      }
    }
  }
}
